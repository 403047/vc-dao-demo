{
  "name": "vc-dao-demo",
  "version": "1.0.0",
  "description": "Decentralized Venture Capital DAO",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-all.js --network coston",
    "test": "hardhat test",
    "node": "hardhat node",
    "frontend": "cd frontend && npm run dev",
    "clean": "hardhat clean",
    "auto-executor": "node scripts/auto-executor.js",
    "deploy-and-start": "npm run deploy && npm run auto-executor"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-waffle": "^2.0.6",
    "chai": "^4.3.10",
    "ethereum-waffle": "^4.0.10",
    "ethers": "^5.7.2",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1"
  },
  "private": true,
  "scripts": {
    "start": "node ./scripts/start.js",
    "dev": "node ./scripts/start.js",
    "build": "echo \"Run your framework build (react-scripts build | vite build | next build)\""
  }
}